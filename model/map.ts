export class Map{
    public id: number;
    public tag: number;
    public list: number;
    public mode: number;

    constructor(id: number, tag: number, list: number, mode: number){
        this.id = id;
        this.tag = tag;
        this.list = list;
        this.mode = mode;
    }

}